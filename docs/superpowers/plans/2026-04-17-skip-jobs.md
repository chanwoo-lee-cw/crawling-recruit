
# Skip Jobs Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 사용자가 지원하지 않을 공고를 `skip_jobs` MCP 툴로 제외 표시하면, 이후 `get_unapplied_jobs`와 `get_job_candidates` 결과에서 자동으로 제외된다.

**Architecture:** `job_skips` 테이블을 신규 생성해 제외 공고를 저장한다. `get_unapplied_job_rows`와 `get_unapplied_jobs` 쿼리에 `JobSkip` LEFT OUTER JOIN 필터를 추가한다. `get_job_candidates`는 `get_unapplied_job_rows`를 경유하므로 별도 수정 불필요.

**Tech Stack:** Python 3.11, SQLAlchemy 2.x (`DeclarativeBase`, `Session`, `insert().on_duplicate_key_update()`), FastMCP, MySQL, pytest

**Spec:** `docs/superpowers/specs/2026-04-17-skip-jobs-design.md`

---

## File Map

| 파일 | 변경 유형 | 내용 |
|------|-----------|------|
| `db/models.py` | 수정 | `JobSkip` ORM 클래스 추가, `Job`에 `skip` relationship |
| `tests/test_db.py` | 수정 | `JobSkip` 테이블 컬럼 검증 테스트 추가 |
| `services/job_service.py` | 수정 | import에 `JobSkip` 추가; `skip_jobs` 메서드 추가; `get_unapplied_job_rows`/`get_unapplied_jobs` skip 필터 추가 |
| `tests/test_job_service.py` | 수정 | 3개 테스트 추가 |
| `tools/skip_jobs.py` | 신규 생성 | MCP 툴 |
| `main.py` | 수정 | 툴 등록 |
| `tests/test_tools.py` | 수정 | 1개 테스트 추가 |

---

### Task 1: `db/models.py` — JobSkip ORM 클래스 추가

**Files:**
- Modify: `db/models.py`
- Modify: `tests/test_db.py`

- [ ] **Step 1: `db/models.py`에 `JobSkip` 클래스 추가**

`db/models.py` 파일 끝에 추가:

```python
class JobSkip(Base):
    __tablename__ = "job_skips"

    job_id: Mapped[int] = mapped_column(
        Integer, ForeignKey("jobs.id", ondelete="CASCADE"), primary_key=True
    )
    reason: Mapped[Optional[str]] = mapped_column(String(255))
    skipped_at: Mapped[datetime] = mapped_column(DateTime, nullable=False)

    job: Mapped["Job"] = relationship(back_populates="skip")
```

`Job` 클래스 내 relationship 목록에 추가 (`applications` relationship 아래):

```python
skip: Mapped[Optional["JobSkip"]] = relationship(back_populates="job", uselist=False)
```

- [ ] **Step 2: import 확인**

```bash
python -c "from db.models import Job, Application, JobDetail, SearchPreset, JobSkip; print('OK')"
```

Expected: `OK`

- [ ] **Step 3: `tests/test_db.py`에 테스트 추가**

`tests/test_db.py` 파일 끝에 추가:

```python
def test_job_skips_table_columns():
    from db.models import JobSkip
    col_names = {c.name for c in JobSkip.__table__.columns}
    assert col_names == {"job_id", "reason", "skipped_at"}
```

- [ ] **Step 4: 테스트 실행**

```bash
pytest tests/test_db.py -v
```

Expected: 6 tests PASS (기존 5개 + 신규 1개)

- [ ] **Step 5: Commit**

```bash
git add db/models.py tests/test_db.py
git commit -m "feat: JobSkip ORM 클래스 추가 (job_skips 테이블)"
```

---

### Task 2: `services/job_service.py` — skip_jobs 메서드 + 쿼리 수정

**Files:**
- Modify: `services/job_service.py`
- Modify: `tests/test_job_service.py`

- [ ] **Step 1: import에 `JobSkip` 추가**

`services/job_service.py` 상단의 import 줄:

```python
# 변경 전
from db.models import Job, Application, JobDetail as OrmJobDetail, SearchPreset

# 변경 후
from db.models import Job, Application, JobDetail as OrmJobDetail, SearchPreset, JobSkip
```

- [ ] **Step 2: `tests/test_job_service.py`에 `skip_jobs` 테스트 2개 추가**

파일 끝에 추가:

```python
def test_skip_jobs_calls_execute():
    mock_engine = MagicMock()
    with patch("services.job_service.Session") as MockSession:
        mock_session = MagicMock()
        MockSession.return_value.__enter__ = MagicMock(return_value=mock_session)
        MockSession.return_value.__exit__ = MagicMock(return_value=False)
        mock_session.execute.return_value = MagicMock()

        service = JobService(engine=mock_engine)
        result = service.skip_jobs([101, 102], reason="연봉 낮음")

    assert mock_session.execute.called
    assert mock_session.commit.called
    assert "2개 공고 제외 완료" in result
    assert "연봉 낮음" in result


def test_skip_jobs_empty_list():
    service = JobService(engine=MagicMock())
    result = service.skip_jobs([])
    assert result == "제외할 공고 ID를 입력해주세요."
```

- [ ] **Step 3: 테스트 실행 → FAIL 확인**

```bash
pytest tests/test_job_service.py::test_skip_jobs_calls_execute \
       tests/test_job_service.py::test_skip_jobs_empty_list -v
```

Expected: 2 tests FAIL (`skip_jobs` 미구현)

- [ ] **Step 4: `skip_jobs` 메서드 구현**

`services/job_service.py`에서 `save_preset` 메서드 앞에 추가:

```python
def skip_jobs(self, job_ids: list[int], reason: str | None = None) -> str:
    if not job_ids:
        return "제외할 공고 ID를 입력해주세요."
    now = datetime.now(timezone.utc).replace(tzinfo=None)
    rows = [{"job_id": jid, "reason": reason, "skipped_at": now} for jid in job_ids]
    with Session(self.engine) as session:
        stmt = insert(JobSkip.__table__).values(rows)
        upsert_stmt = stmt.on_duplicate_key_update(
            reason=stmt.inserted.reason,
            skipped_at=stmt.inserted.skipped_at,
        )
        session.execute(upsert_stmt)
        session.commit()
    suffix = f" (사유: {reason})" if reason else ""
    return f"{len(job_ids)}개 공고 제외 완료{suffix}"
```

- [ ] **Step 5: 테스트 실행 → PASS 확인**

```bash
pytest tests/test_job_service.py::test_skip_jobs_calls_execute \
       tests/test_job_service.py::test_skip_jobs_empty_list -v
```

Expected: 2 tests PASS

- [ ] **Step 6: `tests/test_job_service.py`에 쿼리 필터 테스트 2개 추가**

파일 끝에 추가:

```python
def test_get_unapplied_job_rows_with_skip_join():
    mock_engine = MagicMock()
    with patch("services.job_service.Session") as MockSession:
        mock_session = MagicMock()
        MockSession.return_value.__enter__ = MagicMock(return_value=mock_session)
        MockSession.return_value.__exit__ = MagicMock(return_value=False)

        mock_session.execute.return_value.mappings.return_value.all.return_value = [
            {
                "id": 1001, "company_name": "테스트컴퍼니", "title": "Backend Engineer",
                "location": "서울", "employment_type": "regular",
                "requirements": None, "preferred_points": None,
                "skill_tags": None, "fetched_at": None,
            }
        ]

        service = JobService(engine=mock_engine)
        rows = service.get_unapplied_job_rows()

    assert isinstance(rows, list)
    assert len(rows) == 1
    assert rows[0].id == 1001


def test_get_unapplied_jobs_with_skip_join():
    mock_engine = MagicMock()
    with patch("services.job_service.Session") as MockSession:
        mock_session = MagicMock()
        MockSession.return_value.__enter__ = MagicMock(return_value=mock_session)
        MockSession.return_value.__exit__ = MagicMock(return_value=False)

        mock_session.execute.return_value.mappings.return_value.all.return_value = [
            {"id": 1001, "company_name": "테스트컴퍼니", "title": "Backend Engineer",
             "location": "서울", "employment_type": "regular"}
        ]

        service = JobService(engine=mock_engine)
        result = service.get_unapplied_jobs()

    assert "테스트컴퍼니" in result
    assert "https://www.wanted.co.kr/wd/1001" in result
```

- [ ] **Step 7: `get_unapplied_job_rows` 쿼리에 skip 필터 추가**

`services/job_service.py`의 `get_unapplied_job_rows` 메서드에서 `.outerjoin(Application, ...)` 줄 다음에 추가:

```python
.outerjoin(JobSkip, Job.id == JobSkip.job_id)
```

그리고 `.where(Application.job_id.is_(None))` 줄 다음에 추가:

```python
.where(JobSkip.job_id.is_(None))
```

- [ ] **Step 8: `get_unapplied_jobs` 쿼리에 skip 필터 추가**

`services/job_service.py`의 `get_unapplied_jobs` 메서드에서 `.outerjoin(Application, ...)` 줄 다음에 추가:

```python
.outerjoin(JobSkip, Job.id == JobSkip.job_id)
```

그리고 `.where(Application.job_id.is_(None))` 줄 다음에 추가:

```python
.where(JobSkip.job_id.is_(None))
```

- [ ] **Step 9: 전체 테스트 실행**

```bash
pytest tests/test_job_service.py -v
```

Expected: 전체 PASS

- [ ] **Step 10: Commit**

```bash
git add services/job_service.py tests/test_job_service.py
git commit -m "feat: skip_jobs 메서드 추가 및 get_unapplied_job_rows/get_unapplied_jobs skip 필터 적용"
```

---

### Task 3: `tools/skip_jobs.py` 신규 생성 + `main.py` 등록

**Files:**
- Create: `tools/skip_jobs.py`
- Modify: `main.py`
- Modify: `tests/test_tools.py`

- [ ] **Step 1: `tests/test_tools.py`에 테스트 추가**

파일 끝에 추가:

```python
def test_skip_jobs_tool_calls_service():
    with patch("tools.skip_jobs.get_engine"), \
         patch("tools.skip_jobs.JobService") as MockService:

        mock_service = MagicMock()
        mock_service.skip_jobs.return_value = "2개 공고 제외 완료 (사유: 연봉 낮음)"
        MockService.return_value = mock_service

        from tools.skip_jobs import skip_jobs
        result = skip_jobs(job_ids=[101, 102], reason="연봉 낮음")

    mock_service.skip_jobs.assert_called_once_with([101, 102], "연봉 낮음")
    assert "2개 공고 제외 완료" in result
```

- [ ] **Step 2: 테스트 실행 → FAIL 확인**

```bash
pytest tests/test_tools.py::test_skip_jobs_tool_calls_service -v
```

Expected: FAIL (`tools.skip_jobs` 모듈 없음)

- [ ] **Step 3: `tools/skip_jobs.py` 생성**

```python
from db.connection import get_engine
from services.job_service import JobService


def skip_jobs(job_ids: list[int], reason: str | None = None) -> str:
    """공고를 제외 목록에 추가. 이후 get_unapplied_jobs, get_job_candidates 결과에서 제외됨.

    job_ids: 제외할 공고 ID 목록
    reason: 제외 사유 (선택. 예: "연봉 낮음", "기술스택 불일치")
    """
    try:
        engine = get_engine()
        service = JobService(engine)
        return service.skip_jobs(job_ids, reason)
    except Exception as e:
        return f"오류가 발생했습니다: {e}"
```

- [ ] **Step 4: 테스트 실행 → PASS 확인**

```bash
pytest tests/test_tools.py::test_skip_jobs_tool_calls_service -v
```

Expected: PASS

- [ ] **Step 5: `main.py`에 툴 등록**

```python
# 변경 전 (상단 import 섹션)
from tools.get_job_candidates import get_job_candidates

# 변경 후
from tools.get_job_candidates import get_job_candidates
from tools.skip_jobs import skip_jobs
```

```python
# 변경 전 (툴 등록 섹션)
mcp.tool()(get_job_candidates)

# 변경 후
mcp.tool()(get_job_candidates)
mcp.tool()(skip_jobs)
```

- [ ] **Step 6: import 확인**

```bash
python -c "from main import mcp; print('OK')"
```

Expected: `OK`

- [ ] **Step 7: 전체 테스트 실행**

```bash
pytest tests/ -v
```

Expected: 전체 PASS

- [ ] **Step 8: Commit**

```bash
git add tools/skip_jobs.py main.py tests/test_tools.py
git commit -m "feat: skip_jobs MCP 툴 추가 및 main.py 등록"
```
